# Previous Models

This folder archives older experiments, earlier notebooks, and replaced model outputs.

The active deployment scripts now live in `code files/ml/deployment_models`.
The active deployment artifacts now live in `code files/data/gold/modeling/deployment_models`.
